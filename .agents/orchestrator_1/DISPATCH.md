## 2026-09-19T12:22:47Z

Execute a full multi-agent team workflow (launch multi-path parallel exploration, structured decomposition, and comprehensive refactoring) to harden the `sm86_smooth` proxy runtime (`version.dll`) to resolve real-world game crashes (GitHub Issue #1: unsafe swapchain wrapper pointer dereference, missing early file logger, ResizeBuffers lifecycle failures) and eliminate presentation flicker (GitHub Issue #2: vblank pacing between dual presents).

Specific Requirements:
1. R1. Safe SwapChain Wrapper Identification & Fault-Tolerant Inspection: Eliminate blind pointer dereferencing of `[swap + 0x18]`. Accurately determine whether `IDXGISwapChain` is legitimately wrapped by `NvPresent64.dll` by comparing its vtable pointer against the known proxy vtable RVA (`base + 0x1d3228`), wrapping probing in structured memory/exception safeguards (`VirtualQuery` or `__try / __except`). Non-NvPresent swapchains or third-party interposers (Streamline `sl.interposer.dll`, Reflex, Agility SDK) must gracefully pass through without `0xC0000005` access violations.
2. R2. Early Persistent File Logging: Implement immediate, high-reliability file logger (`logs\sm86_proxy_<pid>.log`) initialized at `DllMain` entry and startup thread launch. Ensure all lifecycle milestones are written and flushed immediately to disk.
3. R3. VBlank Pacing for Dual Presents: In dual-frame presentation mode, query and cache `IDXGIOutput` via `IDXGISwapChain::GetContainingOutput` and invoke `output->WaitForVBlank()` between the two presents so each frame lands on an adjacent VBlank interval, eliminating ~refresh/2 high-frequency flicker.
4. R4. SwapChain ResizeBuffers Lifecycle Handling: Intercept `IDXGISwapChain::ResizeBuffers` (vtable slot 13) and `ResizeBuffers1` (vtable slot 39). Cleanly invalidate cached output pointers, wrapper handles, and internal texture associations on resize, re-establishing smoothly on subsequent `Present()`.
5. R5. Complete Build & Hardware Regression Verification: Ensure full codebase cleanly compiles under MSVC C++17 via `build.bat` (producing `version.dll`, `nvp_live_test.exe`, `nvp_perf_bench.exe`, `proxytest.exe`, `vfi_selftest.exe`) with zero errors. Run `nvp_live_test.exe` on local RTX 3080 to verify live CUDA graph launches and Smooth Motion frame generation execute without regression.
